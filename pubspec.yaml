name: shield_check_mali
description: "Application ShieldCheck Mali"

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  device_information: ^0.0.4

flutter:
  uses-material-design: true
